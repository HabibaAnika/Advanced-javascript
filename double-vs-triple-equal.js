// if in that condition equal is used double so that condition can't catch the type of variable's value it will catch just the value // if used triple this can done
const first = 2;
const second = "2";
if(first == second){  
    console.log("condition is true");
}
else{
    console.log("condition is false");
}
// if one value is 0 and one value is false on that case condition will be true// 
let age = 0;
let age2 = false;
if (age == age2) {
   console.log("true"); 
} else {
    console.log("false");
}
// if one value is 1 and one value is true on that case condition will be true//
let isHe = 1;
let isHe1 = true;
if (isHe == isHe1) {
    console.log("true");
} else {
    console.log("false");
}
// note :: when you make a condition you should try to use triple equal not a single or double equal